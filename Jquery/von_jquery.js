$=jQUery;
var imgs = jQuery("img");
imgs.forEach(function(v){ 
	var 
if (imgs.attr("src")){
	imgSrcArray.push(jQuery(imgs));
	return imgSrcArray.length;
}
});


var imgs = $("img"); 
var images = []; 
$.each(imgs, function(k,v){ 
	images.push(
		$(v).attr("src")
		);
});


var imgs = $("img");
$("img").[0] ("img");
$("img").get(0);
