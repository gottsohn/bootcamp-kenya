var PrimeChecker = function(number) {
  this.number = number;
  this.isPrime = function() {
  
  var prime = true;
  if (number) {
  
 for (var i = 2; i < number; i++) {
      if (number % i === 0) {
        prime = false;
        
      }
   }
 return prime;
}
else 
      return false;
};
};